# 18. Procesy a spouštění programů v systému Linux

>[!question] Procesy a spouštění programů v systému Linux
>Spouštění programů na popředí a na pozadí; pojem proces, ukončení a pozastavení procesu na popředí a na pozadí (fg, bg, & , sekvenční a konkurenční spouštění úloh, rodičovský proces a potomek, skupina procesů a procesy patřící do jedné session,identifikace procesů (číslo úlohy, PID, PPID, PGID, SID), zabíjení procesů (kill ) a signály ( TERM, KILL, STOP, CONT, HUP ), informace o procesech (jobs, ps, top), priorita procesů (nice, renice), plánované spouštění úloh (at, cron, anacron - konfigurace, konfigurační soubory); význam souborového systému /proc, spouštění procesů pod jinou identitou (su, sudo).

---

## Definice

>[!info] Program
>Soubor obsahující spustitelný kód

>[!info] Proces
>Instance programu/spuštěný program
>
>Každý proces má ID = PID
>Program může mít více procesů/instancí

---

## Vypsání procesu

Pro vypsání procesů máme běžně 2-3 příkazy

1. `ps` - vypíše aktivní procesy pro daného uživatele
2. `top` - CLI obdoba task manageru
3. `jobs` - užitečný při práci s procesy v pozadí

----

## Proces - spouštění, ukončení, pozastavení, popředí, pozadí

Každý příkaz (`dnf`, `nano`,...) v Linuxu spouští proces, tedy instanci programu. `dnf` a jiné příkazy jsou v backendu `alias`, které odkazují na spustitelný soubor (soubor s možností `x` - `execute`)

Představme si 2 ilustrační programy, který budou každou sekundu vypisovat slovo

```bash
# program1
# /root/ahoj

while true
do
	echo ahoj
	sleep 1
done
```

```bash
# program2
# /root/nazdar

while true
do
	echo nazdar
	sleep 1
done
```

*Předpokládáme, že soubor má již nastavený pravá `x`*

### Spouštění

Pro jednoduché spuštění `program1` bez `alias` použijeme následující příkaz

```bash
/root/ahoj
```

Tímto jsme spustili příkaz v popředí, čili v aktuální instanci terminálu (`tty`) nemůžeme dělat nic jiného.

---

### (Po)Zastavení

Abychom mohli opět používat aktuální `tty`, tak máme 2 možnosti, jak program přerušit

1. Pozastavení - `CTRL` + `Z`
	- Proces se dočasně přeruší
	- Pomocí příkazu `jobs` uvidíme jeho stav a ID
2. Ukončení - `CTRL` + `C`
	- Proces se ukončí trvale

Pro obnovení pozastaveného procesu použijem příkaz

```bash
fg %[ID]
```

Tedy pokud jsme pozastavili jen jeden proces tak napíšeme

```bash
fg %1
```

---

### Popředí, Pozadí

Tímto se dostáváme ke spouštění procesů v popředí a pozadí

Bylo již zmíněno, jak se spustí příkaz v popředí (viz [[#Spouštění]]). Abychom spustili příkaz na pozadí, použijem příkaz

```bash
/root/ahoj &
```

Zásadní rozdíl mezi procesem v popředí a pozadí je, že když je proces v pozadí, tak lze používat aktuální `tty`, narozdíl od procesu v popředí

Pokud má proces output v `tty`, tak bude akorát náročnější napsat souvislejší string, protože proces bude vizuálně přepisovat terminál

*Př: příkaz `jobs` s procesem v pozadí a outputem v `tty`*

![Processses](/SPS/imgs/Q18/background-proc.png)

Pokud je proces spuštěn v pozadí a chceme ho přenést do popředí popužijeme tento příkaz (předpoklad - beží pouze jeden proces)

```bash
fg %1
```

Pokud je příkaz v popředí a chceme ho přenést do pozadí, učiníme buď ukončením (`CTRL`+`C`) a potom spuštěním na pozadí, nebo pozastavením (`CTRL`+`Z`) a potom příkazem

(Předpoklad - běží pouze jeden proces)
```bash
bg %1
```

---

### Sekvenční spouštění

#### `;`

>[!example] Příklad
>```bash
/root/ahoj ; /root/nazdar
>```

Projdeme si postupně co příkaz dělá

1. Spustí se program `/root/ahoj`
2. `;` čeká než, se program `/root/ahoj` ukončí aby spustil další příkaz
	- `/root/ahoj` je infinite loop, takže ho budeme muset ukončit ručně `CTRL`+`C`
3. Spustí se program `/root/nazdar`

---

#### `&&`

>[!example] Příklad
>```bash
>true && echo ahoj
>-> ahoj
>
>false && echo ahoj
>->
>```

`&&` hlídá, jestli příkaz před ním vrátí Success (true) nebo Error (false).
Pokud vrátí Success, tak provede příkaz po něm.
Pokud vrátí Error, tak příkaz po něm se neprovede.

---

#### `||`

>[!example] Příklad
> ```bash
> true || echo ahoj
> ->
>
> false || echo ahoj
> -> ahoj
> ```

`||` je přesně opačná situace od `&&`, kdy zde to provede další příkaz pokud předcházející příkaz vrátil Error místo Success

---

### Paralelní spouštení

Paralelní spouštění znamená, že najednou spustíme více procesů => z logiky věci musí běžet minimálně *počet procesů - 1* v pozadí

Učiníme tak následovně

```bash
/root/ahoj & /root/nazdar    # pouze program1 běží v pozadí

/root/ahoj & /root/nazdar &  # oba programy běží v pozadí
```

---

## Parent-child procesy

Vztah typu parent-child mezi procesy vzniká ve chvíli, kdy již existující proces použije tzv. `fork()` system call, který spůstí 2+ podprocesy.

>[!example] Příklad
>Kopírujeme soubor ze složky A do složky B a tento přenos je zabalen do jednoho procesu.
>Po procesu chci, aby přenesl soubor a zároveň aby ukazoval progress bar.
>Rodičovský proces tedy zavolá `fork()` a ten vytvoří dva potomky:
>- Potomek1 - přenos dat
>- Potomek2 - progress bar
>
>V moment co skončí přenos dat, tak se ukončí potomek1 i potomek2 a následně rodičovský proces
>
>*Protože progress bar a přenos dat jsou na sobě zavislý, nedává smysl aby pro toto existovali dva normalní procesy, ale je lepší přesně tento případ kdy 2 podprocesy jsou zastřešeny pod jedním hlavním (viz [článek](https://www.geeksforgeeks.org/dsa/difference-between-process-parent-process-and-child-process/))*

---

## PGID, PPID, SID

### PGID

Process Group ID = ID skupiny procesů

Skupina procesů se používa třeba u tzv. `pipeline` , což je většinou řetězeč příkazů, který jsou na sobě  závislý

>[!example] Příklad
>```
>sysctl -a | grep ip_forward
>
>->
>net.ipv4.ip_forward = 0
>net.ipv4.ip_forward_update_priority = 1
>net.ipv4.ip_forward_use_mtu = 0
>```

Často se vyznačuje tím že obsahuje pravě `|` (pipe), ale `pipeline` může být i příkaz sám o sobě.

Důvod proč se seskupují procesy je kvůli řízení jejich chodu - máte víc procesů na sobě závislých, takže dávý smysl, že s ukončením (`CTRL`+`C`) jednoho procesu chcete ukončit i zbytek v té stejné skupině.

---

### PPID

Parent Process ID = ID rodičovského proces

ID, které říká jaký proces je rodičovský, resp. který ho spustil

---

### SID

Session ID = ID relace

Session je nadmnožina skupiny procesů, tzn. zastřešuje skupiny procesů, který jsou spolu propojený

---

### Diagram relací, skupin a procesů

![Process relations diagram](/SPS/imgs/Q18/process-group-session.png)

SID 100 - zastřešuje skupiny procesů, který jsou na sobe závislý
PGID 100 - proces bash (příkazový řádek - uživatelský input)
PGID 200 a 300 - `pipeline`
PID 200 a 300 - první proces si načte všechny soubory v adresáři a místo vypsání předá output procesu 201/301
PID 201 a 301 - vemou input, spočítaj počet řádků, číslo vypíšou do terminálu (bash)

---

## `kill`

Příkaz `kill` se používá k ukončení procesu. Má několik parametrů ale ty nejdůležijtěší jsou:
- `-L` - vypsání možných signálů
- `-s` - signál, tzn. určení, jak se proces ukončí

>[!example] Syntax
>```
>kill -s [signál] [PID]
>```

Každý signál dělá něco jiného a tedy ukončuje proces jinak rychle.

Nejsilnější signál je 9, tedy `SIGKILL` (`KILL`). Tento signál bez diskuze okamžitě ukončuje proces.

Mírnější verze je 15, tedy `SIGTERM` (`TERM`). Narozdíl od `KILL` dává prostor, aby se proces korektně ukončil, tzv. graceful stop

Máme také možnost proces pouze pozastavit a to pomocí signálu 19,  `SIGSTOP` (`STOP`)

Na to je vázaný signál 18, `SIGCONT` (`CONT`), který opět pustí daný proces

Při odhlášení se použíje signál 1, `SIGHUP` (`HUP`), kdy dojde k odpojení procesu od parent procesu

Poslední co stojí za zmínku je 2, `SIGINT` (`INT`), který napodobuje `CTRL`+`C`

---

## Priorita procesů

Priorita určuje pořadí, v jakém budou procesy zpracovány procesorem a přístup k RAM

Priorita má rozsah -20 až 20, kdy -20 je nejvyšší priorita

Pro nastavení priority procesu máme příkaz `nice`

>[!example] Syntax
>```
>nice [-n priorita] [příkaz]
>```

Pro změnu priority existujícího procesu existuje příkaz `renice`

>[!example] Syntax
>```
>renice [-n priorita] [PID]
>```

---

## Plánování spouštění procesů

### `at`

Nejjednodušší plánovaní procesů je pomocí `at`. Tento příkaz úmožní planovat spuštění jednoho příkazu v určitý čas. Jednoduchý, fronta s příkazy je uchována i po rebootu ale slouží jen pro jednorázovou operaci.

>[!example] Syntax
>```
>at [čas]
>
># Otevře se "nová příkazová řádka"
>
>at> [Pipeline / Program ke spuštění]
># CTRL+D pro uložení a ukončení
>```

---

### `crontab`

`crontab` umožňuje opakované spouštění proces podle uživatelských potřeb. Interval je zde velmi flexibilní, lze pracovat od sekund až po roky, dají se přeskakovat jednotlivé hodnoty (př. každý druhý den), specifikovat pouze některé dny apod.

Nejjednodušší způsob jak naplánovat úlohu tímto způsobem je pomocí

```
crontab -e
```

tento příkaz vytvoří ve složce `/var/spool/cron/` soubor s názvem uživatele, kde jsou uložený právě uživatelský úlohy, které se budou jemu spouštět

Další varianta je úprava souboru `/etc/crontab`, kde můžete nastavit, pod jakým uživatelem se bude `crontab` spouštět, ale jinak je nastavení pořád stejné

>[!example] Příklad `crontab -e`
>```
>.------------------ minuta - 0-59
>|   .-------------- hodina - 0-23
>|   |   .---------- den v měsíci - 0-31
>|   |   |   .------ měsíc - 1-12 / jan,feb,mar,...
>|   |   |   |   .-- den v týdnu - 0-6 / 1-7 / mon,tue,wed,...
>|   |   |   |   |
>10  *   *   12  mon   echo Vanoce se blizi > /dev/tty*
>@reboot systemctl stop firewalld.service
>```

První řádek by měl každých 10 minut, každé pondělí v prosinci napsat do každého terminálu `Vanoce se blizi`
Druhý řádek by měl při bootu stopnout firewall

>[!example] Příklad `/etc/crontab`
>```
>.------------------ minuta - 0-59
>|   .-------------- hodina - 0-23
>|   |   .---------- den v měsíci - 0-31
>|   |   |   .------ měsíc - 1-12 / jan,feb,mar,...
>|   |   |   |   .-- den v týdnu - 0-6 / 1-7 / mon,tue,wed,...
>|   |   |   |   |
>10  *   *   12  mon   [uživatel]   echo Vanoce se blizi > /dev/tty*
>@reboot   [uživatel]   systemctl stop firewalld.service
>```

v `/etc/crontab` to funguje stejně, akorát se musí připsat uživatel, pod kterým se úloha bude spouštět

---

### `anacron`

Poslední tu máme `anacron`. Ve své podstatě funguje stejně jako `crontab`, ale na rozdíl od něj umí poznat jestli byl systém vypnutý, tzn. jestli úloha neproběhla a případně spustit úlohu hned po bootu

Konfigurace zde probíha v souboru `/etc/anacrontab`

>[!example] `/etc/anacrontab`
>```
>.------------------ interval ve dnech
>|   .-------------- spoždění spuštění v minutách
>|   |   .---------- identifikátor úlohy
>|   |   |   .------ příkaz
>|   |   |   |
>
>1   5   update    dnf update -y
>```

Tato úloha by měla jednou denně načíst, stáhnout a nainstalovat updaty

---

## `/proc`

Tato složka, resp. souborový systém obsahuje data a složky pro všechny běžící procesy na zařízení.

---

## `su`, `sudo`

`su` = SuperUser
`sudo` = SuperUser DO

Pomocí příkazu `su` jsme schopný přepnout běžné uživatelské rozhraní do tzv. `root` rozhraní. Výhoda tohoto rozhraní je, že zde máte absolutní a neomezná práva, můžete číst a upravovat jakékoliv soubory na systému a měnit nastavení.

Pokud chcem provést jeden příkaz nebo jednu  `pipeline`, tak nám často stačí přidat na začátek řádku příkaz `sudo`, díky kterému se na daný proces budeme tvářit jako SuperUser.

>[!warning] Pozor
>Pro tyto příkazy musí být normální uživatel ve skupině `sudoers` nebo `wheel`
